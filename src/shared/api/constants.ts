export enum RequestType {
  READ = 'READ',
  READ_ONE = 'READ_ONE',
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
}

export enum Table {
  MEMBERS = 'people',
  PROJECTS = 'projects',
  BLOGS = 'blogs',
}
